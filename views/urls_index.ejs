<!DOCTYPE html>
<html lang="en">
<head>
        <% include ./partials/_head %>     
  <title>TinyApp</title>
</head>


<body>

    <h1> All URLs </h1>

        <h1><% urls %></h1>    
    
        <ul>
    <% for (var shortURL in urls) { %>
        <li><%= shortURL %> <%= urls[shortURL].longURL %> <form method="POST" action=<%= `/urls/${shortURL}/delete` %>>
            <button type="submit">DELETE</button></form>
            <form method="GET" action=<%= `/urls/${shortURL}` %>>
            <button type="submit">EDIT</button></form></li>         
    <% } %>
    </ul>
     


    <form method="GET"  action= <%=`/urls/new` %>>    
                <label for="login"> If you want to convert a long link into a short link </label>
                <button type="submit">CONVERT</button></form>
                
    
</body>
</html>
